# audio/

The benchmark prompt:

> "Tell me about a time you led a contentious technical decision."

Recorded as a 16 kHz mono WAV, ~12.4 seconds, by a native British-English speaker. Drop into BlackHole as the system audio source so every copilot under test receives the exact same bytes.

`question-behavioural-12s.wav` to be added before the first public release. Reach out to manu@wayanerd.co.uk if you want the WAV before the GitHub mirror lands.
